# /home/ragan/.config/tmuxinator/foundry.yml

name: foundry

root: ~/projects/r20-foundry/

windows:
  - editor: vim .
  - log: ggsync && yarn start
  - foundry:
      layout: main-horizontal
      panes:
        - foundryvtt
        - ./compile.sh
