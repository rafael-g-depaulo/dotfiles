# /home/ragan/.config/tmuxinator/test-project.yml

name: tui
root: ~/projects/load-tui/

windows:
  - vim:
      layout: main-vertical
      panes:
        - vim ./src/index.ts
        -  # empty
  - console:
      layout: main-vertical
      panes:
        - yarn build-watch
        -  #empty
