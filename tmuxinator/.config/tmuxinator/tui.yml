# /home/ragan/.config/tmuxinator/test-project.yml

name: tui
root: ~/projects/load-tui/

on_project_start: nvm use

windows:
  - vim:
      layout: main-vertical
      panes:
        - vim ./src/app.tsx
        -  # yarn dev
  - console:
      layout: main-vertical
      panes:
        -  # yarn build-watch
