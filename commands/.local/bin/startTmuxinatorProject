#!/bin/bash

# Get project name
PROJECT_NAME=$(
  tmuxinator list |
    tail -n +2 |
    perl -pe 's/(\w+)\s+/$1\n/g' |
    fzf --preview "batcat --color=always $HOME/.config/tmuxinator/{}.yml"
)

tmuxinator start "$PROJECT_NAME"
