#!/bin/bash

WARNING_RPM=500
DANGER_RPM=200

RPM=$(getFanRpm)

if [[ $RPM -le $DANGER_RPM ]]; then
        # soundAlarm &
	echo "%{F#f00}$RPM%{F-}"
else
	if [[ $RPM -le $WARNING_RPM ]]; then
		echo "%{F#ff0}$RPM%{F-}"
	else
		echo "$RPM"
	fi
fi
